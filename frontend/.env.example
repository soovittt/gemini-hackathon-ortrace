# API base URL (backend). Leave unset to use defaults:
# - Local (npm run dev): http://localhost:3000
# - Production build: https://prod-ortrace-api-jtjotridxa-uc.a.run.app
# VITE_API_URL=http://localhost:3000

# Domain split: landing = ortrace.com, app = app.ortrace.com
# Only set these in production so localhost runs the full app.
# VITE_APP_ORIGIN=https://app.ortrace.com
# VITE_LANDING_HOSTS=ortrace.com,www.ortrace.com
